# ========================
# VcaniTrade AI - Requirements
# Server (Vast.ai Ubuntu) + Client (Windows 11)
# ========================

# --- Core Framework ---
# PyQt6 6.11.0 is crashing in Qt6Core.dll on this Windows/RPA setup.
# Keep the desktop runtime on the stable 6.7 line until this is re-tested.
PyQt6==6.7.1
PyQt6-Qt6==6.7.3
aiohttp>=3.9.0
requests>=2.31.0

# --- Data & Math ---
numpy>=2.3.0
pandas>=2.0.0
ta>=0.10.0

# --- AI / LLM ---
google-genai>=1.0.0
openai>=1.12.0
pydantic>=2.5.0

# --- Markets & Finance ---
ccxt>=4.0.0
holidays>=0.39
yfinance>=0.2.40
cffi>=2.0.0

# --- Browser Automation (DEPRECATED — legacy execution path) ---
# RETIRED from the order-execution critical path. Playwright/pyautogui drove the
# TradingView Desktop UI to click Buy/Sell, which is too slow (800ms-3s) and too
# fragile for Apex's real-time trailing drawdown. See AUDIT.md findings F1-F2.
# Live orders now go through tradovate-middleware/ (TradingView webhook ->
# Tradovate REST). These deps are kept ONLY so the legacy dashboard/research UI
# still imports cleanly. They will be deleted once the middleware has run clean
# for 30 days (AUDIT.md migration step 6).
# Install the new middleware separately: pip install -r tradovate-middleware/requirements.txt
playwright>=1.40.0
beautifulsoup4>=4.12.0

# --- Desktop / Vision ---
Pillow>=10.0.0
pyautogui>=0.9.54
mss>=9.0.0
pygetwindow>=0.0.9
dxcam>=0.0.5; platform_system=="Windows"

# --- MetaTrader 5 (Windows only — MT5 has no Mac/Linux Python connector) ---
MetaTrader5>=5.0.45; platform_system=="Windows"

# --- Config ---
python-dotenv>=1.0.0
